SocialMediaApp
│
├── app
│   ├── _layout.tsx
│   └── index.tsx
│
├── assets
├── node_modules
├── app.json
├── package.json
└── index.js

Step : 1

5️⃣ Create Entry File (Required for Expo Router)

Create file in root folder

index.js

Add:

import "expo-router/entry";

This loads file-based routing from the app folder.

6️⃣ Setup Root Layout

File:

app/_layout.tsx

Code:

import { Stack } from "expo-router";

export default function RootLayout() {
  return <Stack />;
}

This enables navigation stack.

7️⃣ Create Home Screen

File:

app/index.tsx

Code:

import { StyleSheet, Text, View } from "react-native";

export default function HomeScreen() {
  return (
    <View style={styles.container}>
      <Text style={styles.text}>
        Welcome to My First React Native App 🚀
      </Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },
  text: {
    fontSize: 22,
    fontWeight: "bold",
  },
});
8️⃣ Clear Cache (If Screen Not Loading)

Run:

npx expo start -c

This clears Metro cache.

9️⃣ Final Result

App screen will display:

Welcome to My First React Native App 🚀

in the center of the screen.

🔟 Important Concepts
Android	React Native
Activity	Screen
XML Layout	JSX
RecyclerView	FlatList
Intent	router.push()
Fragment	Nested routes
Navigation Example
import { router } from "expo-router";

router.push("/profile");


SocialMediaApp
│
├── app
│   ├── _layout.tsx
│   │
│   ├── (auth)
│   │     ├── login.tsx
│   │     └── register.tsx
│   │
│   ├── (tabs)
│   │     ├── _layout.tsx
│   │     ├── home.tsx
│   │     ├── search.tsx
│   │     ├── profile.tsx
│   │     └── settings.tsx
│   │
│   └── posts
│         ├── index.tsx
│         └── [id].tsx
│
├── components
│     ├── Button.tsx
│     ├── Card.tsx
│     └── Header.tsx
│
├── services
│     ├── api.ts
│     └── authService.ts
│
├── hooks
│     └── useAuth.ts
│
├── store
│     └── userStore.ts
│
├── utils
│     └── helpers.ts
│
├── assets
│     ├── images
│     └── icons
│
├── index.js
├── app.json
└── package.json
